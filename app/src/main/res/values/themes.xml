<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.CeylonBotanica" parent="Theme.Material3.Light.NoActionBar">
        <!-- Brand palette -->
        <item name="colorPrimary">@color/cb_green_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">@color/cb_green_dark</item>
        <item name="colorSecondary">@color/cb_green_dark</item>
        <item name="colorTertiary">@color/cb_green_dark</item>

        <!-- optional chrome -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>